---
type: implementation-notes
version: 1.0.0
status: draft
created: "{{date}}"
expert: developer
iteration: {{iteration}}
---

# Implementation Notes: {{project_name}}

## Overview

This document provides guidance for implementing the architecture defined in the ADRs.

## Implementation Order

### 1. Project Setup

**Tasks:**
- [ ] Initialize project with chosen framework
- [ ] Configure TypeScript/linting
- [ ] Set up environment variables
- [ ] Configure database connection

**Files to create:**
```
/
├── package.json
├── tsconfig.json
├── .env.example
├── .gitignore
└── src/
    └── index.ts
```

### 2. Database Layer

**Tasks:**
- [ ] Define schema
- [ ] Create migrations
- [ ] Set up ORM/query builder
- [ ] Create seed data

**Key patterns:**

```typescript
// Example: Prisma schema pattern
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

### 3. Authentication

**Tasks:**
- [ ] Configure auth provider
- [ ] Implement login/logout flow
- [ ] Set up protected routes
- [ ] Add session management

**Key patterns:**

```typescript
// Example: Auth middleware pattern
const requireAuth = async (req, res, next) => {
  const token = req.headers.authorization?.split(' ')[1];
  if (!token) return res.status(401).json({ error: 'Unauthorized' });

  try {
    const user = await verifyToken(token);
    req.user = user;
    next();
  } catch {
    res.status(401).json({ error: 'Invalid token' });
  }
};
```

### 4. API Layer

**Tasks:**
- [ ] Set up route structure
- [ ] Implement core endpoints
- [ ] Add validation
- [ ] Configure error handling

**Key patterns:**

```typescript
// Example: Controller pattern
class UserController {
  async getUser(req: Request, res: Response) {
    const { id } = req.params;
    const user = await userService.findById(id);
    if (!user) return res.status(404).json({ error: 'Not found' });
    res.json(user);
  }
}
```

### 5. Frontend Components

**Tasks:**
- [ ] Set up component library
- [ ] Create layout components
- [ ] Implement core pages
- [ ] Add state management

**Key patterns:**

```typescript
// Example: Component pattern
interface ButtonProps {
  variant: 'primary' | 'secondary';
  children: React.ReactNode;
  onClick?: () => void;
}

export function Button({ variant, children, onClick }: ButtonProps) {
  return (
    <button className={styles[variant]} onClick={onClick}>
      {children}
    </button>
  );
}
```

## Common Pitfalls

### 1. [Pitfall Name]
**Problem:** [Description]
**Solution:** [How to avoid/fix]

### 2. [Pitfall Name]
**Problem:** [Description]
**Solution:** [How to avoid/fix]

## Testing Strategy

| Layer | Tool | Coverage Target |
|-------|------|-----------------|
| Unit | Vitest | 80% |
| Integration | Vitest | Key flows |
| E2E | Playwright | Critical paths |

## Deployment Checklist

- [ ] Environment variables configured
- [ ] Database migrations run
- [ ] SSL certificates valid
- [ ] Monitoring configured
- [ ] Backup strategy in place

---

*Generated by NoodleCrew Developer*
